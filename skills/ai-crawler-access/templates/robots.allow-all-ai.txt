# robots.allow-all-ai.txt
# Intura Skills -- ai-crawler-access
# Goal: MAXIMIZE AI visibility (recommended for most sites doing GEO).
# All AI crawlers, AI search-index bots, and live user-fetch agents are welcome,
# in addition to traditional search engines. Training is also allowed.
#
# How to use: merge these rules into your site's existing robots.txt. KEEP any
# existing Disallow rules you have for private areas (e.g. /admin, /cart, /api).
# Replace example.com with your domain.

# --- Default: allow everything (this alone already permits every AI + search bot) ---
User-agent: *
Disallow:

# --- Explicit welcome for the major AI bots (optional; for clarity/intent) ---
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Applebot
User-agent: Amazonbot
User-agent: Meta-ExternalAgent
User-agent: Meta-ExternalFetcher
User-agent: CCBot
User-agent: cohere-ai
User-agent: Diffbot
User-agent: Timpibot
Allow: /

# Note: Google-Extended and Applebot-Extended are training opt-out tokens.
# We intentionally do NOT disallow them here, so AI training stays allowed too.

# Help AI engines and search engines find your sitemap:
Sitemap: https://example.com/sitemap.xml

# Recommended next step: publish https://example.com/llms.txt
# (run the llms-txt skill) so AI engines get a clean, machine-readable summary.
